apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-ui-app
  namespace: argocd-mukul
spec:
  project: default
  source:
    repoURL: https://github.com/mukul403/k8s_week6.git
    path: Week6/Argo-UI-Manifest
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: argo-manual-ui-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
